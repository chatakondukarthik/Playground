#include<stdio.h>
int main()
{
  int a,b;
  scanf("%d%d",&a,&b);
  if(b==8)
    printf("Raj can fix both painting");
  else
    printf("Raj cannot fix both painting");
  return 0;
  //fill your code
}